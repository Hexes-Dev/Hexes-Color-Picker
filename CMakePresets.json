{
    "version": 3,
    "configurePresets": [
        {
            "name": "default",
            "description": "Default settings for color picker",
            "hidden": false,
            "binaryDir": "${workspaceFolder}/build",
            "cacheVariables": {
                "CMAKE_BUILD_TYPE": "Debug"
            }
        },
        {
            "name": "release",
            "description": "Release settings for color picker",
            "hidden": false,
            "binaryDir": "${workspaceFolder}/build",
            "cacheVariables": {
                "CMAKE_BUILD_TYPE": "release"
            }
        }

    ]
}